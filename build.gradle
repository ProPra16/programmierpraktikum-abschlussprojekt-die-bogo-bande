apply plugin: 'java'
apply plugin: 'application'

mainClassName = "Main"

def global_version = '1.0'

repositories{
    mavenCentral()
}

dependencies {
    compile group: 'junit', name: 'junit', version: '4.11'
    compile group: 'de.hhu.stups', name: 'virtual-kata-lib', version: '1.0.0'
    compile group: 'org.apache.commons', name: 'commons-io', version: '1.3.2'
}

jar {
    baseName = 'TDDT'
    manifest {
        attributes(
                'Main-Class': 'Main',
        )
    }
}

task RunneableJar(type: Jar) {
    manifest.from jar.manifest
    baseName ="TDDT-"+global_version
    from {
        configurations.runtime.collect { it.isDirectory() ? it : zipTree(it) }
    } with jar
}

task manageBuild{
    delete 'build/libs/TDDT-'+global_version+'.jar'
    copy {
        from 'build/libs/TDDT-'+global_version+'.jar'
        into 'build/'
    }
}

artifacts {
    archives RunneableJar
}